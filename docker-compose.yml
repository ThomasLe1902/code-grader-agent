version: "3.9"

services:
  code-grader-agent:
    build: .
    container_name: code-grader-agent
    ports:
      - "8000:8000"
    volumes:
      - .:/app  # hot-reload for development
    command: uvicorn app:app --host 0.0.0.0 --port 8000 --reload
